package com.cyberschnitzel.Domain.Validators;

import com.cyberschnitzel.Domain.Entities.Donator;

public class DonatorValidator implements Validator<Donator> {
    @Override
    public boolean validate(Donator entity) {
        return true; // TODO: Proper validation
    }
}
